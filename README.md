# practicing-javascript-project01

Episode project 01 JavaScript

## 📁 Estrutura do Projeto

Este projeto começa com a criação dos três arquivos principais que compõem a base de uma página web utilizando HTML, CSS e JavaScript:

- **index.html**: Arquivo principal da página. Contém a estrutura e o conteúdo base do site.
- **style.css**: Responsável por definir o estilo da página — cores, fontes, espaçamento, etc.
- **index.js**: Contém a lógica em JavaScript. Aqui são definidos os comportamentos e interações da página.

A combinação desses três arquivos forma uma estrutura mínima e funcional para projetos web.

## Como visualizar

1. Abra o arquivo `index.html` em um navegador (duplo clique ou `start index.html` no terminal).
2. Certifique-se de que os arquivos `style.css` e `index.js` estão na mesma pasta e corretamente vinculados ao HTML.
